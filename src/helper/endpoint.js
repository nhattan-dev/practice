const Login = "/login"
const Home = "/"
const Admin = "/admin"
const User = "/user"
const Search = "/search"
const Cart = "/cart"
const Product = "/product/:keyword"

// eslint-disable-next-line
export default {
    Login,
    Home,
    Admin,
    User,
    Search,
    Cart,
    Product,
}